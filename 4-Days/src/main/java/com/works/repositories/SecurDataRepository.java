package com.works.repositories;

import com.works.props.SecurData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SecurDataRepository extends JpaRepository<SecurData, Integer> {
}
